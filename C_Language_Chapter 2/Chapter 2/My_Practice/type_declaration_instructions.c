#include <stdio.h>

int main()
{
    // int a = 4; // Type declaration instruction
    // int a = 4, b, c; // Type declaration instruction
    // b = c = a;
    // printf("The value of a is %d\n", a); // Output : The value of a is 4
    // printf("The value of b is %d\n", b); // Output : The value of b is 4
    // printf("The value of c is %d\n", c); // Output : The value of c is

    // float b = a + 8.9; // it will throw error because we are using a without declaring a
    float a = 1.1;

    return 0;
}