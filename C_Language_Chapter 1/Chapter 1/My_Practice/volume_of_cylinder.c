#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.14;
    float area = pi * radius * radius;
    int height = 3;
    printf("Volume of cylinder is %f", area * height);
    return 0;
}