#include <stdio.h>

/*
multi line comment
This is my first c program
*/

int main()
{
    int tom;
    int Tom;
    int error_code;
    printf("Hello Uday");
    return 0;
}

// single line comment
// run gcc first.c to run the application then we will get a.exe file in the same path. After we have to give directly that file name a
// gcc first.c -o first.exe ---> this give the .exe file with the same name of .c
// VS code extensions used c/C++, code runner