#include <stdio.h>

int main()
{
    printf("Hello Uday");
    return 0;
}

// run gcc first.c to run the application then we will get a.exe file in the same path. After we have to give directly that file name a